# Data

Place the raw dataset at:

- `data/household_power_consumption.csv`

This repository ignores `data/household_power_consumption.csv` by default to avoid committing large/private files.

For reviewers, a small sample file is included:

- `data/sample_household_power_consumption.csv`
